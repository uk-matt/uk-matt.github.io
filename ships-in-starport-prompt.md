This text will create a set of random spaceships which will be used in a sci-fi role-playing game. The output will be an HTML file which includes some JavaScript.
The outline is that the script will be given a starport type. Then dice will be simulated to determine first the number of rolls to be made. 
Then that number of rolls will be made on a list. The list used will match the starport type.
